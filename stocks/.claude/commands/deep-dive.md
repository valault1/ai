Run a qualitative deep-dive for: **$ARGUMENTS**

The ticker is `$ARGUMENTS`. Assume the quantitative screen has already been run and `reports/$ARGUMENTS/$ARGUMENTS_quant.json` exists. If it doesn't, run the quant screen first using `processes/01-quantitative-screen.md`.

1. Follow `processes/02-qualitative-analysis.md` for all four sub-steps.
2. Follow `processes/03-final-verdict.md` to synthesize the final verdict.
3. Present the verdict, scores, and elevator pitch to the user.
